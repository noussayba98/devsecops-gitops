apiVersion: v1
kind: Service

metadata:
  name: node-service
  labels:
    app: node-service

spec:
  type: ClusterIP

  selector:
    app: node-service

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
